import { assert } from 'chai';
import { ethers } from 'hardhat';

describe('Om', function (): void {
  // const tenant = ethers.provider.getSigner(0);
  // const landlord = ethers.provider.getSigner(1);
});

